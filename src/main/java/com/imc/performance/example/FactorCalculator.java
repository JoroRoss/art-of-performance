package com.imc.performance.example;

class FactorCalculator {

    double computeFactor(double aValue, double aPrice) {
        // in reality this is also quite a slow calculation.
        return aValue * aPrice;
    }
}
